[core]
color = blue
display_modules = distro(), uname(n), uname(r), uptime(), wm(), de(), env(colors), env(font), packages(), ram(), cpu(), env(terminal), env(term_shell), env(editor), env(pager), env(browser), fs(/), mpd(albums)
align = top

