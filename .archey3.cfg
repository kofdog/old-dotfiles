[core]
color = blue
display_modules = distro(), uname(n), uname(r), uptime(), wm(), env(colors), env(font), packages(), ram(), env(terminal), env(editor), env(pager), env(browser), fs(/), mpd(albums)
align = top
