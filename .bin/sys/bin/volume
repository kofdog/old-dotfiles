#!/bin/bash

direction=${1:-down}
case $direction in
    up    ) amixer set Master $2%+ unmute;;
    down  ) amixer set Master $2%- unmute;;
    mute  ) amixer set Master toggle;;
    *     ) ;;
esac
